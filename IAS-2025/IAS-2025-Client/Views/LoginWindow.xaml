<Window x:Class="IAS_2025_Client.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:material="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="IAS-2025 IDE - Login" Height="500" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True">

    <Border CornerRadius="15">
        <Border.Background>
            <LinearGradientBrush EndPoint="1,1" StartPoint="0,0">
                <GradientStop Color="#1a1a2e" Offset="0"/>
                <GradientStop Color="#16213e" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>

        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Logo -->
            <TextBlock Grid.Row="0" Text="IAS-2025" FontSize="32" FontWeight="Bold"
                       Foreground="{StaticResource MaterialDesignPrimaryHueLightBrush}"
                       HorizontalAlignment="Center" Margin="0,20,0,10"/>

            <TextBlock Grid.Row="1" Text="Login to continue" FontSize="14"
                       Foreground="{StaticResource MaterialDesignBodyLightBrush}"
                       HorizontalAlignment="Center" Margin="0,0,0,30"/>

            <!-- Username -->
            <TextBox x:Name="UsernameBox" Grid.Row="2" material:HintAssist.Hint="Username"
                     Style="{StaticResource MaterialDesignFilledTextBox}"
                     Margin="0,10" FontSize="14" Height="50"/>

            <!-- Password -->
            <PasswordBox x:Name="PasswordBox" Grid.Row="3" material:HintAssist.Hint="Password"
                         Style="{StaticResource MaterialDesignFilledPasswordBox}"
                         Margin="0,10" FontSize="14" Height="50"/>

            <!-- Login Button -->
            <Button x:Name="LoginButton" Grid.Row="4" Content="LOGIN"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="{StaticResource MaterialDesignPrimaryHueLightBrush}"
                    Foreground="White" FontWeight="Bold" Height="45"
                    Margin="0,20,0,10" Cursor="Hand"/>

            <!-- Register Link -->
            <TextBlock x:Name="RegisterLink" Grid.Row="5" Text="Don't have an account? Register"
                       Foreground="{StaticResource MaterialDesignBodyLightBrush}"
                       HorizontalAlignment="Center" VerticalAlignment="Bottom"
                       Cursor="Hand" TextDecorations="Underline"
                       FontSize="12" Margin="0,0,0,20"/>

            <!-- Loading Overlay -->
            <Border x:Name="LoadingOverlay" Grid.RowSpan="6" Background="#CC1a1a2e"
                    Visibility="Collapsed" CornerRadius="15">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <material:ProgressRing IsActive="True" Width="50" Height="50"
                                          Foreground="{StaticResource MaterialDesignPrimaryHueLightBrush}"/>
                    <TextBlock Text="Logging in..." Margin="0,10,0,0"
                               Foreground="White" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>